prefix + 파일명 + suffix

-> /WEB-INF/views/home.jsp

RequestMapping과 GetMapping/PostMapping의 차이
- RequestMapping은 클래스와 메소드에서 모두 사용.
- Get/PostMapping은 메소드에서만 사용.
>> RequestMapping을 class에서 사용하는 경우 : 







uri: 식별자
url: 위치 
./ 현위치 , ../ 상위폴더